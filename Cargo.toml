[package]
name    = "enciphers"
version = "0.1.0"
edition = "2021"

[lib]
name       = "enciphers"
crate-type = ["cdylib"]

[dependencies]
encipher = "0.1.0"
pyo3     = { version = "0.29", features = ["extension-module"] }

[profile.release]
opt-level     = 3
lto           = true
codegen-units = 1
